{
  "name": "local-auth-server",
  "version": "1.0.0",
  "description": "Authorization server to be used with your local clients. Supports OAuth2.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sachingodishela",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2"
  }
}
